# AML webhook Lambda dependencies (Python 3.11)
# boto3 is provided by the Lambda runtime; pinned here for local testing parity.
boto3>=1.34,<2.0
# pyarrow is large; in Lambda it is supplied via a layer or container image
# (see template.yaml -> Layers / packaging notes), not bundled in the function zip.
pyarrow>=15.0,<18.0
