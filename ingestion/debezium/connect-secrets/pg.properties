# LOCAL-ONLY credentials for the Debezium source, read by Kafka Connect's
# FileConfigProvider (see docker-compose.yml). These are throwaway values for the
# local docker-compose Postgres — NOT a real secret. In dev/pre/prod the connector
# reads credentials from the AWS Secrets Manager config provider on MSK Connect
# (see README.md), and this file does not exist.
username=credit_app
password=local-only-not-a-real-secret
